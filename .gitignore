# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Secrets / local config
.env
.vscode/

# Personal planning docs — kept out of the repo
DESIGN.md
DESIGN.pdf
NOTES.md
VOCABULARY.md
WH40KDC.md

# scratch / learning sample
sample.py
sample_roster.py

# Test-fixture rosters — exported army lists embed GW's rules text; keep out of the repo.
# Drop your own ListForge exports here to run the combat tests (see tests/fixtures/README.md).
tests/fixtures/*.json

# Personal exploration rosters (for scripts/show_matrix.py) — also embed GW rules text.
rosters/

# Local inspection / "run it and look" utilities — kept out of the repo for now.
# Easy to un-ignore later; hard to un-commit. Revisit if they become portfolio-worthy.
tools/
